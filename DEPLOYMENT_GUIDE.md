# 跨裝置共享設置指南 📱☁️

讓你和女友可以在不同裝置（手機、電腦）上一起使用，即時同步更新！

## 🎯 需要完成的兩個步驟

### 步驟 A：部署網站到線上（讓大家都能訪問）
### 步驟 B：設置 Firebase 雲端資料庫（即時同步資料）

## 📋 詳細操作步驟

---

## 步驟 A：部署到 GitHub Pages（10 分鐘）

### A1. 建立 GitHub 帳號（如果還沒有）
1. 前往 https://github.com/
2. 點擊 "Sign up"
3. 註冊帳號

### A2. 建立新的 Repository
1. 登入 GitHub 後，點擊右上角 ➕ → "New repository"
2. 填寫資訊：
   - **Repository name**: `Lin_Sheep_Trip` （或任何你喜歡的名字）
   - **Description**: `小羊的東京旅行規劃`
   - **Public** 或 **Private**：選 Public（免費）
   - ❌ 不要勾選 "Add a README file"
3. 點擊 "Create repository"

### A3. 上傳程式碼到 GitHub

**方法 1：使用 Git（推薦）**

在命令提示字元執行：

```bash
cd E:\Data\juniperlin\Desktop\Lin_Sheep_Trip

# 初始化 Git
git init

# 新增所有檔案
git add .

# 提交
git commit -m "Initial commit"

# 設定遠端倉庫（替換成你的 GitHub 用戶名）
git remote add origin https://github.com/你的用戶名/Lin_Sheep_Trip.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**方法 2：使用 GitHub 網頁上傳**

如果 Git 指令失敗，可以直接在 GitHub 網頁上傳：
1. 在 Repository 頁面點擊 "uploading an existing file"
2. 將 `Lin_Sheep_Trip` 資料夾內的所有檔案拖曳上傳
3. 點擊 "Commit changes"

### A4. 啟用 GitHub Pages

1. 在你的 Repository 頁面，點擊 **Settings**
2. 左側選單點擊 **Pages**
3. 在 "Source" 下：
   - Branch: 選擇 **main**
   - Folder: 選擇 **/ (root)**
4. 點擊 **Save**
5. 等待 1-2 分鐘，會看到網址：
   ```
   https://你的用戶名.github.io/Lin_Sheep_Trip/
   ```

✅ **完成！** 現在你和女友都可以用這個網址訪問了！

---

## 步驟 B：設置 Firebase 雲端同步（15 分鐘）

### B1. 建立 Firebase 專案

1. **前往 Firebase Console**
   - 訪問 https://console.firebase.google.com/
   - 使用 Google 帳號登入

2. **建立新專案**
   - 點擊 "新增專案" 或 "Add project"
   - 專案名稱：`lin-sheep-trip`（或任何名稱）
   - 點擊 "繼續"
   - 停用 Google Analytics（可選）
   - 點擊 "建立專案"
   - 等待建立完成

### B2. 建立 Realtime Database

1. 在左側選單點擊 **"Realtime Database"**
2. 點擊 **"建立資料庫"**
3. 選擇位置：**asia-southeast1**（新加坡，離台灣最近）
4. 安全性規則選擇：**"以測試模式啟動"**（之後可以改）
5. 點擊 **"啟用"**

### B3. 建立 Storage（用於圖片）

1. 在左側選單點擊 **"Storage"**
2. 點擊 **"開始使用"**
3. 安全性規則選擇：**"以測試模式啟動"**
4. 選擇位置：**asia-southeast1**
5. 點擊 **"完成"**

### B4. 取得 Firebase 設定

1. 點擊左上角的 **⚙️ 專案設定**
2. 往下滾動到 **"您的應用程式"**
3. 點擊 **Web 圖示** `</>`
4. 應用程式暱稱：`Lin Sheep Trip`
5. ✅ 勾選 "同時為此應用程式設定 Firebase Hosting"
6. 點擊 **"註冊應用程式"**
7. **複製 firebaseConfig** 的內容，看起來像這樣：

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "lin-sheep-trip.firebaseapp.com",
  databaseURL: "https://lin-sheep-trip-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "lin-sheep-trip",
  storageBucket: "lin-sheep-trip.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

### B5. 更新程式碼

我會幫你更新檔案，你只需要：

1. 將上面複製的 `firebaseConfig` 告訴我
2. 我會更新 `firebase-config.js` 和 `index.html`
3. 重新推送到 GitHub

---

## 🎉 完成後的效果

✅ **網址分享**：`https://你的用戶名.github.io/Lin_Sheep_Trip/`
✅ **手機訪問**：兩個人都用手機打開這個網址
✅ **即時同步**：你更新 → 女友立刻看到、女友更新 → 你立刻看到
✅ **永久保存**：資料儲存在 Firebase，永遠不會消失
✅ **跨裝置**：手機、平板、電腦都能用

---

## 📱 使用情境

**你在家用電腦**：
1. 打開網址
2. 新增明天的行程
3. 上傳照片

**女友在公司用手機**：
1. 打開同一個網址
2. 立刻看到你剛新增的行程！
3. 也可以編輯和新增

**雙向即時同步** ☁️✨

---

## 🔐 安全性設定（可選）

如果想要只有你們兩個能編輯：

1. 在 Firebase Console → Realtime Database → 規則
2. 改成：
```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

之後可以加入身份驗證，只允許特定用戶編輯。

---

## 下一步

**現在你需要做的：**

1. **完成步驟 A**（GitHub Pages 部署）
   - 如果不會用 Git，告訴我，我會給你更詳細的步驟
   
2. **完成步驟 B1-B4**（建立 Firebase 並取得設定）
   - 完成後把 `firebaseConfig` 給我

3. **我會幫你更新程式碼**
   - 整合 Firebase
   - 重新部署

**準備好了嗎？** 從步驟 A 開始，或者告訴我你卡在哪裡，我會協助你！😊
